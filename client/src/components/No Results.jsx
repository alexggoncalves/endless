const NotFound = () => {
    return (
        <div className="no-results center">
            <span>Sorry, but we couldn’t find anything matching your query. </span>
            <span>{':('}</span>
        </div>
    );
};

export default NotFound
